# Shared environment file for docker-compose

# Database configuration (used by postgres image and backend)
DB_HOST=database           # service name inside Docker network; do not change
DB_USER=hostelops_user
DB_PASSWORD=strongpassword
DB_NAME=hostelops_db

# JWT secret for signing tokens (keep secret in real deployments)
JWT_SECRET=supersecretjwtkey

# Backend configuration
PORT=4000
NODE_ENV=production

